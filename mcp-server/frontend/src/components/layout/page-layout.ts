/** Shared page width tokens — wide pages use full desktop canvas. */
export const PAGE_SHELL_WIDE = "mx-auto min-w-0 w-full max-w-[min(100%,90rem)]";
export const PAGE_SHELL_SETTINGS = "mx-auto min-w-0 w-full max-w-[min(100%,90rem)]";
